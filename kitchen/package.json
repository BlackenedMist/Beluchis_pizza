{
  "name": "beluchis-kitchen",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Headless kitchen printer daemon for Beluchis: polls the site, auto-prints new online orders to a network thermal printer, and marks them preparing.",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file=.env index.mjs",
    "check": "node --check index.mjs && for f in lib/*.mjs; do node --check \"$f\" || exit 1; done"
  }
}